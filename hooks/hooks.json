{
  "description": "ticket-flow: guard the one-way step of making repository contents public, and hold a dispatched ticket agent to a valid verdict",
  "hooks": {
    "PreToolUse": [
      {
        "matcher": "Bash",
        "hooks": [
          {
            "type": "command",
            "command": "${CLAUDE_PLUGIN_ROOT}/hooks/visibility-gate.sh"
          }
        ]
      }
    ],
    "SubagentStop": [
      {
        "matcher": "*",
        "hooks": [
          {
            "type": "command",
            "command": "${CLAUDE_PLUGIN_ROOT}/hooks/verdict-gate.sh"
          }
        ]
      }
    ]
  }
}

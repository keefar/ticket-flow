{
  "description": "ticket-flow: guard the one-way step of making repository contents public",
  "hooks": {
    "PreToolUse": [
      {
        "matcher": "Bash",
        "hooks": [
          {
            "type": "command",
            "command": "${CLAUDE_PLUGIN_ROOT}/hooks/visibility-gate.sh"
          }
        ]
      }
    ]
  }
}

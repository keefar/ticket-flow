{
  "$schema": "https://anthropic.com/claude-code/marketplace.schema.json",
  "name": "ticket-flow",
  "description": "Kanban + git-worktree-based ticket workflow plugin for Claude Code.",
  "owner": { "name": "Christoph Schmid" },
  "plugins": [
    {
      "name": "ticket-flow",
      "description": "Kanban- or beads-backed ticket workflow for Claude Code: /spec → /pickup → /implement → /finish, orchestrated by /flow (--local, --parallel, --spawn), each ticket in its own git worktree.",
      "source": "./"
    }
  ]
}
